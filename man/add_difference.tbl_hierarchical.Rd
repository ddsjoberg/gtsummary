% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_difference.tbl_hierarchical.R
\name{add_difference.tbl_hierarchical}
\alias{add_difference.tbl_hierarchical}
\alias{add_difference.tbl_ard_hierarchical}
\title{Add rate differences to hierarchical tables}
\usage{
\method{add_difference}{tbl_hierarchical}(
  x,
  levels = NULL,
  statistic = "{estimate}\%",
  estimate_fun = label_style_number(digits = 1, scale = 100),
  ...
)

\method{add_difference}{tbl_ard_hierarchical}(
  x,
  levels = NULL,
  statistic = "{estimate}\%",
  estimate_fun = NULL,
  ...
)
}
\arguments{
\item{x}{(\code{tbl_hierarchical}/\code{tbl_ard_hierarchical})\cr
table created with \code{\link[=tbl_hierarchical]{tbl_hierarchical()}} or \code{\link[=tbl_ard_hierarchical]{tbl_ard_hierarchical()}}.}

\item{levels}{(\code{vector})\cr
a length-two vector of the \code{by} variable levels to compare. The difference
is calculated as \code{levels[1]} minus \code{levels[2]}. This argument is required
when the \code{by} variable has more than two levels, and when \code{by} has exactly
two levels it is optional and can be used to flip the direction of the
difference. Default is \code{NULL}.}

\item{statistic}{(\code{string})\cr
a single glue string defining the difference statistic to display. The only
available element is \code{{estimate}} (the rate difference). Default is
\code{"{estimate}\%"}.}

\item{estimate_fun}{(\code{function})\cr
a function to round and format the rate difference. For
\code{add_difference.tbl_hierarchical()} the default is
\code{label_style_number(digits = 1, scale = 100)}. For
\code{add_difference.tbl_ard_hierarchical()} the default is \code{NULL}, meaning the
formatting function carried in the source ARD is used.}

\item{...}{These dots are for future extensions and must be empty.}
}
\value{
a gtsummary table of the same class as \code{x}
}
\description{
Add a column of event-rate differences to a table created with
\code{\link[=tbl_hierarchical]{tbl_hierarchical()}} or \code{\link[=tbl_ard_hierarchical]{tbl_ard_hierarchical()}}. For every node in the
hierarchy (e.g. each system organ class and each preferred term) the event
rate of a second \code{by} group is subtracted from the rate of a first \code{by} group.
This is a wrapper around \code{cards::diff_ard_hierarchical()}.

The table must be stratified by a single \code{by} variable and its statistics must
include the rate (\code{p}) statistic (the default for \code{tbl_hierarchical()}).
}
\examples{
\dontshow{if ((identical(Sys.getenv("NOT_CRAN"), "true") || identical(Sys.getenv("IN_PKGDOWN"), "true"))) withAutoprint(\{ # examplesIf}
# Example 1 ----------------------------------
# rate difference between two treatment arms
ADAE_subset <- cards::ADAE |>
  dplyr::filter(AESOC \%in\% unique(cards::ADAE$AESOC)[1:5]) |>
  dplyr::filter(.by = AESOC, AEDECOD \%in\% unique(cards::ADAE$AESOC)[1:5])

tbl_hierarchical(
  data = ADAE_subset,
  variables = c(AESOC, AEDECOD),
  by = TRTA,
  denominator = cards::ADSL,
  id = USUBJID
) |>
  add_difference(levels = c("Xanomeline High Dose", "Placebo"))
\dontshow{\}) # examplesIf}
}

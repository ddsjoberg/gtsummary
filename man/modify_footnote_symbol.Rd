% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modify_footnote_symbol.R
\name{modify_footnote_symbol}
\alias{modify_footnote_symbol}
\alias{remove_footnote_symbol}
\title{Modify Footnote Symbols}
\usage{
modify_footnote_symbol(x, symbol)

remove_footnote_symbol(x)
}
\arguments{
\item{x}{(\code{gtsummary})\cr
A gtsummary object}

\item{symbol}{(\code{character})\cr
a character vector of length 2 or greater giving the ordered symbols used
to reference footnotes, e.g. \code{c("*", "\\u2020", "\\u2021")}. Symbols are
assigned to footnotes in the order the footnotes appear in the table. When
the number of footnotes exceeds the number of symbols supplied, the symbols
are recycled. A length of at least 2 is required because \code{as_gt()} passes
these to \code{gt::opt_footnote_marks()}, which interprets a single string as
the name of a built-in mark scheme. Currently only utilized by \code{as_gt()}
and \code{as_flex_table()}.}
}
\value{
Updated gtsummary object
}
\description{
\itemize{
\item \code{modify_footnote_symbol()} customizes the symbols used to reference
footnotes in a gtsummary table. By default, footnotes are referenced with
sequential numbers (\verb{1, 2, 3, ...}). This function replaces those numbers
with a user-specified set of symbols, e.g. \code{c("*", "\\u2020", "\\u2021")}
(an asterisk, dagger, and double dagger).
\item \code{remove_footnote_symbol()} resets the footnote reference marks back to the
default numbering.
}
}
\details{
The symbol sequence may also be set for all tables via the
\code{"pkgwide-chr:footnote_symbol"} theme element; a value set with
\code{modify_footnote_symbol()} takes precedence over the theme element.
}
\section{Common footnote marks}{


The table below lists common footnote reference marks and the R strings used
to create them. The six marks correspond to those used in
\code{gt::opt_footnote_marks(marks = "extended")}. Pass the R string (e.g.
\code{"\\u2020"}) to the \code{symbol} argument to obtain the corresponding mark.\tabular{lll}{
   Name \tab Unicode \tab R string \cr
   asterisk \tab \verb{U+002A} \tab \code{"*"} \cr
   dagger \tab \code{U+2020} \tab \code{"\\u2020"} \cr
   double dagger \tab \code{U+2021} \tab \code{"\\u2021"} \cr
   section sign \tab \verb{U+00A7} \tab \code{"\\u00A7"} \cr
   double vertical line \tab \code{U+2016} \tab \code{"\\u2016"} \cr
   pilcrow (paragraph sign) \tab \verb{U+00B6} \tab \code{"\\u00B6"} \cr
}
}

\examples{
\dontshow{if (identical(Sys.getenv("NOT_CRAN"), "true") || identical(Sys.getenv("IN_PKGDOWN"), "true") && gtsummary:::is_pkg_installed("broom", ref = "cardx")) withAutoprint(\{ # examplesIf}
# use symbols (instead of numbers) to reference footnotes
tbl <-
  trial |>
  tbl_summary(by = trt, include = c(age, grade), missing = "no") |>
  add_p() |>
  modify_footnote_symbol(symbol = c("*", "\u2020", "\u2021"))
tbl

# reset footnotes back to the default numbering
tbl |> remove_footnote_symbol()
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=modify_footnote_header]{modify_footnote_header()}}, \code{\link[=modify_footnote_body]{modify_footnote_body()}}, \code{\link[=modify_footnote_spanning_header]{modify_footnote_spanning_header()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_cross_continuous.R
\name{tbl_cross_continuous}
\alias{tbl_cross_continuous}
\title{Continuous cross tabulation}
\usage{
tbl_cross_continuous(
  data,
  con,
  rows,
  col = NULL,
  statistic = "{median} ({p25}, {p75})",
  label = NULL
)
}
\arguments{
\item{data}{A data frame}

\item{con}{Variable name of the continuous column to be summarized}

\item{rows}{Vector of categorical variable names}

\item{col}{Option name of single categorical variable. Default it \code{NULL}}

\item{statistic}{String specifying the continuous summary statistics to
display.  The default is \code{"{median} ({p25}, {p75})"}. See
\code{tbl_summary(statistic=)} argument for details.}

\item{label}{List of formulas specifying variables labels,
e.g. \code{list(age ~ "Age", stage ~ "Path T Stage")}.  If a
variable's label is not specified here, the label attribute
(\code{attr(data$age, "label")}) is used.  If
attribute label is \code{NULL}, the variable name will be used.}
}
\value{
a gtsummary table
}
\description{
Summarize a continuous by categorical variables
}
\section{Example Output}{

\if{html}{Example 1}

\if{html}{\figure{tbl_cross_continuous_ex1.png}{options: width=38\%}}

\if{html}{Example 2}

\if{html}{\figure{tbl_cross_continuous_ex2.png}{options: width=35\%}}
}

\examples{
# Example 1 ----------------------------------
tbl_cross_continuous_ex1 <-
  tbl_cross_continuous(
    data = trial,
    con = age,
    col = trt,
    rows = grade
  )

# Example 2 ----------------------------------
tbl_cross_continuous_ex2 <-
  tbl_cross_continuous(
    data = trial,
    con = age,
    rows = grade
  )
}
